# Generative Galaxy

## Overview

Generative Galaxy is an algorithmic visualization project created with Processing. The program generates a unique space scene containing stars, colorful nebula clouds, planets, orbital paths, a glowing galaxy core, an asteroid belt, and randomly generated comets.

The visualization is based on a generative approach. Random values are used to create different positions, sizes, colors, shapes, orbits, and visual compositions each time the scene is generated. The `noise()` function is also used to create a more organic cosmic dust effect.

## Current Features

- Procedurally generated galaxy scene
- Random star field
- Colorful nebula clouds
- Glowing galaxy core
- Randomly generated planets
- Planet rings
- Planet moons
- Planet surface patterns
- Orbital paths
- Asteroid belt
- Cosmic dust generated with `noise()`
- Randomly generated comets with glowing particle tails
- Screenshot saving functionality

## Controls

- Press `R` to generate a new galaxy.
- Press `S` to save the current visualization as a PNG image.

Saved screenshots are stored inside the `screenshots` folder.

## Software

This project was created with:

- Processing
- Java mode

## Project Purpose

The purpose of this project is to demonstrate algorithmic visualization through a generative approach. The generated images are not manually drawn. Instead, they are produced by algorithms that use randomness and procedural rules to create multiple unique versions of the same visual concept.

The project also demonstrates how additional visual elements can be added step by step. The first version contained the main galaxy structure, while the second version introduces randomly generated comets with glowing tails.

## Screenshots Plan

The project will include at least six screenshots that demonstrate both the base version and the extended version of the visualization:

1. Initial generated galaxy
2. Regenerated galaxy after pressing `R`
3. First extended galaxy version with comet effect
4. Second regenerated galaxy version with comet effect
5. Third regenerated galaxy version with comet effect
6. Fourth regenerated galaxy version with comet effect

More screenshots may be added as the project is extended with additional visual elements.

## Suggested Commit History

A clean GitHub history for the project may look like this:

1. `Initial generative galaxy version`
2. `Add comet effect`
3. `Add final visual improvements`

## Planned Improvements

Future versions may include:

- Falling stars
- More detailed planets
- Additional glow effects
- Improved galaxy composition
- Black hole effect

## Author

Course project for algorithmic visualization through a generative approach.
